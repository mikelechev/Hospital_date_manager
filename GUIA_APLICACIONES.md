# Guía Básica de las Aplicaciones

> Este documento quedó desactualizado (describía una arquitectura de "tres aplicaciones independientes" anterior a `app_unificado.py`, `scripts/triaje.py` y `scripts/medico.py`, y afirmaba que el chatbot seguía usando `chatbot/models/modelo_campeon.json`, ya retirado). Para no mantener dos documentos que se puedan desincronizar otra vez, todo el contenido vive ahora en **[README.md](README.md)**, que cubre los seis puntos de entrada del proyecto, `run.sh`, la configuración del LLM y las entradas de los modelos.

Consulta el README para: cómo instalar (`./run.sh setup`), qué hace cada app (API de reservas, dashboard de simulación, chatbot, portal unificado, vista de triaje, vista de personal clínico), y la tabla de comandos de `run.sh`.
