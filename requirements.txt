fastapi
uvicorn
streamlit
pandas
numpy
xgboost
# Sin pin de versión exacta: models/modelo_definitivo.joblib es un
# VotingClassifier (XGBClassifier + CatBoostClassifier + LogisticRegression,
# con CalibratedClassifierCV) — no usa HistGradientBoostingClassifier ni
# GradientBoostingClassifier, que son las clases internas de scikit-learn
# que reorganizaron su módulo `_loss` entre 1.8 y 1.9 y rompían la carga del
# pickle en versiones distintas. Este modelo no depende de ese módulo, así
# que en principio no hace falta fijarlo a 1.8.0. Antes de confiar en esto
# del todo, comprobar en el venv (que ya tiene 1.9.x): `python -c "import
# joblib; joblib.load('models/modelo_definitivo.joblib')"`.
scikit-learn
joblib
plotly
scipy
catboost
