fastapi
uvicorn
streamlit
pandas
numpy
xgboost
# Pin exacto obligatorio: models/modelo_definitivo.joblib es un
# VotingClassifier(HistGradientBoostingClassifier + RandomForestClassifier)
# envuelto en CalibratedClassifierCV (confirmado cargando el pickle real,
# no es XGBoost+CatBoost+LogisticRegression pese a lo que dice algún
# comentario/doc antiguo). HistGradientBoostingClassifier SÍ depende del
# módulo interno `_loss`, que scikit-learn reorganizó entre 1.8 y 1.9 —
# instalar 1.9.x rompe la carga del pickle con
# `ModuleNotFoundError: '_loss'` en el dashboard, el chatbot y el portal
# de paciente. No quitar este pin sin regenerar el modelo.
scikit-learn==1.8.0
joblib
plotly
scipy
